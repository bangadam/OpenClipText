# .memlog.md — architecture spine run (manual; memlog.py unavailable)

- decision: Stack = Swift native (SwiftUI/AppKit menu bar). Binds: language, UI layer. Prevents: divergence into Electron/JS path. User: "pake swift aja biar native".
- decision: Working mode = Fast path was not chosen; user answered stack directly. Mode still open.
- decision: Purpose/audience question pending.
- decision: Persistence = SQLite via GRDB. Binds: storage layer. Prevents: flat-file divergence, slow search/dedup at scale.
- decision: Clipboard recording = NSPasteboard changeCount polling (~250ms timer). Only mechanism available on macOS.
- assumption: Distribution = local Xcode build, unsigned, Gatekeeper-exempted for personal use. User: "pake sqlite aja" (did not answer mode/deliverable/deployment).
- decision: UI shape = NSMenu quick list (numbers 1-9, 20/page) + separate panel window for search, full history, chunked hover preview. CopyClip-compatible quick UX, panel carries the differentiator.
- assumption: Item size cap = 1MB (aligns NFR-1); larger clipboard contents ignored.
- decision: Excluded apps list stored in UserDefaults (bundle IDs, non-sensitive).
- decision: Paradigm = layered (4 layers: App shell, Domain, Services, Storage). Small app; layers map to Swift modules/packages.
- decision: All decisions logged; drafting spine now.
- event: spine finalized (status final, 2026-09-20)
